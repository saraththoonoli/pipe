import { SalutaionPipePipe } from './salutaion-pipe.pipe';

describe('SalutaionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SalutaionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
